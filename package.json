{
  "name": "employee-info-tracker",
  "version": "1.0.0",
  "description": "Create, Read, Update, and Delete Employees through a JS based CLI interface. ",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "console.table": "0.10.0",
    "dotenv": "^16.4.5",
    "inquirer": "^6.3.1",
    "mysql2": "^3.11.3"
  }
}
